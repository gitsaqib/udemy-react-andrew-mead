var React = require('react')
var ReactDOM = require('react-dom')

var Logo = React.createClass(
  {
      render : function(){
            return (
              <div >
                  Logo
              </div>
            );
      }
  }
);
module.exports = Logo;
